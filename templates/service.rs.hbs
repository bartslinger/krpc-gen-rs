// Generated file DO NOT EDIT
use crate::connection;
use crate::connection::Connection;
use crate::decoder;
use crate::encoder;

use super::schema;

pub struct {{ service_name }}<'s> {
    conn: &'s Connection,
}
impl<'s> {{ service_name }}<'s> {
    pub fn new(conn: &'s Connection) -> {{ service_name }}<'s> {
        {{ service_name }}<'s> {
            conn
        }
    }

    // service methods
    {{#each service_methods as |method|}}

    pub async fn {{ method.name }}(&'s self) -> Result<{{ method.return_type_signature }}, Error> {
    }
    {{/each}}

    // getters
    {{#each service_getters as |getter|}}

    pub async fn {{ getter.name }}(&'s self) -> Result<{{ getter.return_type_signature }}, Error> {
    }
    {{/each}}
    
    // setters
    {{#each service_setters as |setter|}}

    pub async fn {{ setter.name }}(&'s self) -> Result<{{ setter.return_type_signature }}, Error> {
    }
    {{/each}}
}

// Classes
{{#each classes as |class| }}
pub struct {{ class.name }}<'a> {
    id: u64,
    conn: &'a Connection<'a>,
}
impl<'a> {{ class.name }}<'a> {
    // methods
    {{#each class.methods as |method| }}
    async fn {{ method.name }}(&'a self) -> Result<{{ method.return_type_signature }}, Error> {
        
    }

    {{/each}}
    // getters
    {{#each class.getters as |getter| }}
    async fn {{ getter.name }}(&'a self) -> Result<{{ getter.return_type_signature }}, Error> {
        
    }

    {{/each}}
}

{{/each}}